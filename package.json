{
    "name": "sudoku-app",
    "private": true,    
    "workspaces": [
        "apps/*"
    ],
    "scripts": {
      "build": "bun --filter  './apps/*' build",
      "dev": "bun --filter  './apps/*' dev",
      "graphql-gen": "bun run --filter './apps/*' graphql-gen"
    }
  }